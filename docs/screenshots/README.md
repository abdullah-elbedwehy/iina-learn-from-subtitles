# Screenshots

Drop your PNGs here. README.md references:

- `settings.png` — the plugin settings panel in IINA
- `lookup.png` — a real lookup result in the browser

Recommended size: ~1200px wide, 2x retina.

The settings screenshot you already have (the IINA → Settings → Plugins → Subtitle Lookup view) saves perfectly as `settings.png`. For `lookup.png`, take a screenshot of the browser tab right after triggering a lookup so people see the actual AI output format.
